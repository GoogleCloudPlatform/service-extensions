test {
  name: "BasicRequestBodyProcessing 200B x 10Chunks"
  benchmark: true
  request_body {
    input {
      content: "foo should become bar. foo should become bar. foo should "
      "become bar. foo should become bar. foo should become bar. foo should "
      "become bar. foo should become bar. foo should become bar. foo should "
      "become bar."
    }
    result {
      body {
        exact: "bar should become bar. bar should become bar. bar should "
        "become bar. bar should become bar. bar should become bar. bar should "
        "become bar. bar should become bar. bar should become bar. bar should "
        "become bar."
      }
    }
  }
  request_body {
    input {
      content: "When foo, then bar. When foo, then bar. When foo, then bar. "
      "When foo, then bar. When foo, then bar. When foo, then bar. When foo, "
      "then bar. When foo, then bar. When foo, then bar. When foo, then bar."
    }
    result {
      body {
        exact: "When bar, then bar. When bar, then bar. When bar, then bar. "
        "When bar, then bar. When bar, then bar. When bar, then bar. When bar, "
        "then bar. When bar, then bar. When bar, then bar. When bar, then bar."
      }
    }
  }
  request_body {
    input {
      content: "foo should become bar. foo should become bar. foo should "
      "become bar. foo should become bar. foo should become bar. foo should "
      "become bar. foo should become bar. foo should become bar. foo should "
      "become bar."
    }
    result {
      body {
        exact: "bar should become bar. bar should become bar. bar should "
        "become bar. bar should become bar. bar should become bar. bar should "
        "become bar. bar should become bar. bar should become bar. bar should "
        "become bar."
      }
    }
  }
  request_body {
    input {
      content: "foo should become bar. foo should become bar. foo should "
      "become bar. foo should become bar. foo should become bar. foo should "
      "become bar. foo should become bar. foo should become bar. foo should "
      "become bar."
    }
    result {
      body {
        exact: "bar should become bar. bar should become bar. bar should "
        "become bar. bar should become bar. bar should become bar. bar should "
        "become bar. bar should become bar. bar should become bar. bar should "
        "become bar."
      }
    }
  }
  request_body {
    input {
      content: "foo should become bar. foo should become bar. foo should "
      "become bar. foo should become bar. foo should become bar. foo should "
      "become bar. foo should become bar. foo should become bar. foo should "
      "become bar."
    }
    result {
      body {
        exact: "bar should become bar. bar should become bar. bar should "
        "become bar. bar should become bar. bar should become bar. bar should "
        "become bar. bar should become bar. bar should become bar. bar should "
        "become bar."
      }
    }
  }
  request_body {
    input {
      content: "foo should become bar. foo should become bar. foo should "
      "become bar. foo should become bar. foo should become bar. foo should "
      "become bar. foo should become bar. foo should become bar. foo should "
      "become bar."
    }
    result {
      body {
        exact: "bar should become bar. bar should become bar. bar should "
        "become bar. bar should become bar. bar should become bar. bar should "
        "become bar. bar should become bar. bar should become bar. bar should "
        "become bar."
      }
    }
  }
  request_body {
    input {
      content: "foo should become bar. foo should become bar. foo should "
      "become bar. foo should become bar. foo should become bar. foo should "
      "become bar. foo should become bar. foo should become bar. foo should "
      "become bar."
    }
    result {
      body {
        exact: "bar should become bar. bar should become bar. bar should "
        "become bar. bar should become bar. bar should become bar. bar should "
        "become bar. bar should become bar. bar should become bar. bar should "
        "become bar."
      }
    }
  }
  request_body {
    input {
      content: "foo should become bar. foo should become bar. foo should "
      "become bar. foo should become bar. foo should become bar. foo should "
      "become bar. foo should become bar. foo should become bar. foo should "
      "become bar."
    }
    result {
      body {
        exact: "bar should become bar. bar should become bar. bar should "
        "become bar. bar should become bar. bar should become bar. bar should "
        "become bar. bar should become bar. bar should become bar. bar should "
        "become bar."
      }
    }
  }
  request_body {
    input {
      content: "foo should become bar. foo should become bar. foo should "
      "become bar. foo should become bar. foo should become bar. foo should "
      "become bar. foo should become bar. foo should become bar. foo should "
      "become bar."
    }
    result {
      body {
        exact: "bar should become bar. bar should become bar. bar should "
        "become bar. bar should become bar. bar should become bar. bar should "
        "become bar. bar should become bar. bar should become bar. bar should "
        "become bar."
      }
    }
  }
  request_body {
    input {
      content: "foo should become bar. foo should become bar. foo should "
      "become bar. foo should become bar. foo should become bar. foo should "
      "become bar. foo should become bar. foo should become bar. foo should "
      "become bar."
    }
    result {
      body {
        exact: "bar should become bar. bar should become bar. bar should "
        "become bar. bar should become bar. bar should become bar. bar should "
        "become bar. bar should become bar. bar should become bar. bar should "
        "become bar."
      }
    }
  }
  request_body {
    input {
      content: "foo should become bar. foo should become bar. foo should "
      "become bar. foo should become bar. foo should become bar. foo should "
      "become bar. foo should become bar. foo should become bar. foo should "
      "become bar."
    }
    result {
      body {
        exact: "bar should become bar. bar should become bar. bar should "
        "become bar. bar should become bar. bar should become bar. bar should "
        "become bar. bar should become bar. bar should become bar. bar should "
        "become bar."
      }
    }
  }
}
test {
  name: "BasicResponseBodyProcessing 200B x 2Chunks"
  benchmark: true
  response_body {
    input {
      content: "foo should become bar. foo should become bar. foo should "
      "become bar. foo should become bar. foo should become bar. foo should "
      "become bar. foo should become bar. foo should become bar. foo should "
      "become bar."
    }
    result {
      body {
        exact: "bar should become bar. bar should become bar. bar should "
        "become bar. bar should become bar. bar should become bar. bar should "
        "become bar. bar should become bar. bar should become bar. bar should "
        "become bar."
      }
    }
  }
  response_body {
    input {
      content: "When foo, then bar. When foo, then bar. When foo, then bar. "
      "When foo, then bar. When foo, then bar. When foo, then bar. When foo, "
      "then bar. When foo, then bar. When foo, then bar. When foo, then bar."
    }
    result {
      body {
        exact: "When bar, then bar. When bar, then bar. When bar, then bar. "
        "When bar, then bar. When bar, then bar. When bar, then bar. When bar, "
        "then bar. When bar, then bar. When bar, then bar. When bar, then bar."
      }
    }
  }
}
